# LangGraph orchestration (Jetson Thor / PTZ gateway node)
langgraph>=0.2.76
langchain-core>=0.3.74

# LLM backends — install what you use on the Jetson
langchain-ollama>=0.3.0
langchain-anthropic>=0.3.0
langchain-openai>=0.3.0

pyyaml>=6.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Pillow required by simulated PTZ in ptz-agent (stitched panorama)
Pillow>=10.0.0

# MCP bridge (same pattern as MSA-main/ptz-agent/msa_mcp)
mcp>=1.7.0

# Optional hardware PTZ via ptz-agent
# reolink_aio

# Optional — ANL argo-proxy (installed by scripts/local_dev.sh)
# See requirements-argo.txt
